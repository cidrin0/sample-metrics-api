spring:
  application:
    name: metrics-api

  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/product_db
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
  shutdown: graceful

# Metrics
management:
  endpoints:
    enabled-by-default: false # desabilita background metricas
    web:
      exposure:
        include: "health,prometheus,metrics" # expoem o endpoint so das metricas habilitadas
  endpoint:
    prometheus:
      enabled: true
    metrics:
      enabled: true
    health:
      enabled: true
      show-details: always
  server:
    port: 8080 # porta do health/prometheus (heartbeat api)